<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:background="@color/backgroud_transparent_bg"
    android:gravity="center">

    <com.framework.greendroid.imagepicker.picker.CustGridView
        android:id="@+id/gridGallery"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="top"
        android:layout_margin="@dimen/gridview_padding"
        android:cacheColorHint="@color/backgroud_transparent_bg"
        android:listSelector="@color/backgroud_transparent_bg"
        android:numColumns="4"
        tools:listitem="@layout/picker_gallery_item"
        android:descendantFocusability="beforeDescendants"
        />

    <ImageView
        android:id="@+id/imgNoMedia"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="@dimen/gridview_padding"
        android:contentDescription="@string/app_name" />

</FrameLayout>